import { FunctionComponent, ReactNode } from 'react';
interface IAppLayout {
    children: ReactNode;
}
/**
 * Mocks the chrome of an app running on consoles.redhat.com
 */
export declare const AppLayout: FunctionComponent<IAppLayout>;
export {};
